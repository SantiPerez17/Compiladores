declare.section
    int:a,b,c;
    float:x,y,z;
    bool:r;
enddeclare.section
program.section
    display("hola")
    display("holanda")
    display("ewr") (* donde “ewr” debe ser una cte literal string *)
    display(5.5 + aux)
    display(7 <= mi_var)
    while(f<a) do
        display("hola")
        if (b==c) then
           if (c==f) then
                a:=cola(4;[1,2,3,4,5,c+3])
           else
                a:=-((1+b)*4) or c
           end
        end
    end
endprogram.section
